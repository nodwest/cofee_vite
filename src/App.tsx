import React from 'react'

// sections
import {Header} from '~sections'

// ui
import {Button} from '~ui'

const App: React.FC = () => {
	return (
		<div className={'mt-20 text-grey-200'}>
			<Button label={'asda'} type={'danger'}>asdasdad</Button>
		</div>
	)
}
export default App 